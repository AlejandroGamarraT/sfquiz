# Backend: Spring Boot 3 + Spring Data JPA + Hibernate + MySQL
# Package root: com.sapsf.quiz
# Run: ./mvnw spring-boot:run (port 8080)
# Test: ./mvnw test
# Build: ./mvnw package

# Structure
# controller/ service/ repository/ model/ dto/

# Watch out for
# CORS: allowed origin localhost:4200 defined in CorsConfig.java
# MySQL must be running before boot
