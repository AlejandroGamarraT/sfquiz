# SAP SF Quiz App
# Stack: Spring Boot 3 + MySQL | Angular 17 PWA
# Structure: backend/ frontend/
# Conventions: DTOs always for API responses, never expose JPA entities directly
# correct_option type: char A | B | C | D
# API responses shape: always { data, error }